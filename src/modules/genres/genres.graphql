type Genre {
  id: ID!
  name: String
  description: String
  country: String
  year: Int
}

input GenreInput {
  name: String
  description: String
  country: String
  year: Int
}

type PaginatedReponse {
  items: [Genre]
  offset: Int
  limit: Int
  total: Int
}

# type Query {
#   getAll(): PaginatedReponse
# }
